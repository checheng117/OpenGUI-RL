# Metrics Summaries

`quantitative_summary.md` and `quantitative_summary.json` are small frozen summaries from the final project packaging step.

They are included for inspection and report alignment. To recompute them, users need the external benchmark data and local saved artifacts, then can run:

```bash
python scripts/run_quantitative_metrics_suite.py
```
