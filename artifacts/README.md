# Artifacts

This directory contains small public artifacts copied from the final packaging step:

- `metrics/`: compact quantitative summaries.
- `tables/`: CSV and Markdown tables used by the report and README.

Large experiment outputs, checkpoints, prediction dumps, screenshots, and logs remain under ignored local directories such as `outputs/` and are not redistributed.
